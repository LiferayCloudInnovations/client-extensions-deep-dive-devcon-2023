createDockerContainer {
	hostConfig.network = "host"
}

apply plugin: "org.sonarqube"

sonar {
    properties {
        property "sonar.projectKey", "client-extensions"
        property 'sonar.java.source', 11
    }
}